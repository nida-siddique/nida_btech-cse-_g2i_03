int main() {
    int a,b,c;
    printf("enter 2 numbers");
    scanf("%d %d",&a,&b);
    if(a==b)
    {
       printf("both no are equal") ;
    }
    else
    {
        printf("both no are not equal");
    }
        
    
    
    return 0;
}
