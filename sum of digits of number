#include <stdio.h>

int main() {
    int d,sum=0,a;
    printf("enter a number");
    scanf("%d",&a);
    for(int i=a;i>0;i=i/10)
    {
        d=i%10;
        sum=sum+d;
    }
        
    printf("sum of digit= %d \n",sum);
    
    return 0;
}
