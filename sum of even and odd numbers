#include <stdio.h>

int main() {
    int n,i,se=0,so=0;
    printf("enter number");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        if(i%2==0)
        {
            se=se+i;
        }
else
{
    so=so+i;
}}
printf("\n sum of even number=%d",se);
printf("\n sum of odd number=%d",so);
return 0;
}
